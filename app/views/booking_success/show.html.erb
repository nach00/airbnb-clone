<!DOCTYPE html>
<html>
  <head>
    <title>Booking Success - Airbnb Clone</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', 'data-turbo-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbo-track': 'reload' %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>

  <body>
    <%= render 'layouts/navbar' %>
    
    <div class="container my-5">
      <div class="row justify-content-center">
        <div class="col-md-8">
          <div class="card booking-success-card">
            <div class="card-body text-center p-5">
              <div class="success-icon mb-4">
                <i class="fas fa-check-circle fa-5x text-success"></i>
              </div>
              
              <h1 class="display-4 mb-4">Booking Processing</h1>
              <p class="lead mb-4">Your booking is being processed. You will receive a confirmation email shortly.</p>
              
              <div class="booking-details mt-4">
                <div class="row">
                  <div class="col-md-6 mb-3">
                    <div class="detail-card">
                      <h5>Property</h5>
                      <p class="mb-0"><%= @booking.property.title %></p>
                      <small class="text-muted"><%= @booking.property.city %>, <%= @booking.property.country %></small>
                    </div>
                  </div>
                  
                  <div class="col-md-6 mb-3">
                    <div class="detail-card">
                      <h5>Dates</h5>
                      <p class="mb-0"><%= @booking.start_date.strftime('%B %d, %Y') %> - <%= @booking.end_date.strftime('%B %d, %Y') %></p>
                      <small class="text-muted"><%= @booking.nights %> night<%= 's' if @booking.nights != 1 %></small>
                    </div>
                  </div>
                  
                  <div class="col-md-6 mb-3">
                    <div class="detail-card">
                      <h5>Guests</h5>
                      <p class="mb-0"><%= @booking.guests %> guest<%= 's' if @booking.guests != 1 %></p>
                    </div>
                  </div>
                  
                  <div class="col-md-6 mb-3">
                    <div class="detail-card">
                      <h5>Total Amount</h5>
                      <p class="mb-0 h4">$<%= @booking.total_amount %></p>
                    </div>
                  </div>
                </div>
              </div>
              
              <div class="mt-5">
                <a href="/" class="btn btn-primary btn-lg me-3">Return Home</a>
                <a href="/my-bookings" class="btn btn-outline-primary btn-lg">View My Bookings</a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <script src="https://kit.fontawesome.com/your-fontawesome-kit.js" crossorigin="anonymous"></script>
  </body>
</html>